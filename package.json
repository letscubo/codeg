{
  "name": "codeg",
  "private": true,
  "version": "0.30.10-23",
  "packageManager": "pnpm@11.9.0",
  "comment": "This fork ships no frontend — the Next.js app under src/ was removed (the platform has its own UI and only ever calls codeg's HTTP API). Node is kept for exactly two jobs: the Tauri CLI, which release.yml uses to minisign the server bundle, and the sidecar staging script.",
  "scripts": {
    "tauri": "tauri",
    "tauri:prepare-sidecars": "node src-tauri/scripts/prepare-sidecars.mjs",
    "tauri:before-dev": "pnpm tauri:prepare-sidecars",
    "tauri:before-build": "pnpm tauri:prepare-sidecars",
    "server:build": "cd src-tauri && cargo build --release --bin codeg-server --no-default-features",
    "server:dev": "cd src-tauri && cargo run --bin codeg-server --no-default-features"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
