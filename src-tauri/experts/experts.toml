# 内置专家技能清单 —— MyClaw fork 里**刻意留空**。
#
# 上游在这里内嵌 14 个软件工程方法论技能(TDD / 系统化调试 / 写计划…),science.toml
# 另有 13 个科研技能。MyClaw 的技能改由平台下发(见 commands/myclaw_skills.rs):
# 内容存在平台的 myclaw_skills 表,容器定时拉取,改一句文案不必发版。
#
# 两处 `ensure_central_*_installed()` 调用已从启动路径移除,所以本文件实际不会被
# 加载;保留空清单与空 skills/ 目录是为了 `include_dir!` 的路径仍然成立,同时把
# 与上游的分歧限制在数据层,减少同步时的冲突面。
#
# ExpertsTomlRoot 的 `expert` 字段带 #[serde(default)],空清单解析为空 Vec,不会报错。
